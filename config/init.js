/**
 * This file handles basic setup...anything that needs to run before the app is
 * loaded (note that this runs even before the classes for the app are defined).
 */

// promisify Composer, makes things much nicer to work with
Composer.promisify();

